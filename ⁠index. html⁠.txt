<!DOCTYPE html>
<html lang="ar" dir="rtl" class="dark scroll-smooth">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>اختبار حركات الذكاء الاصطناعي السينمائية</title>

    <!-- Tailwind CSS CDN -->
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            darkMode: 'class',
            theme: {
                extend: {
                    colors: {
                        brand: {
                            cyan: '#00f2fe',
                            violet: '#4facfe',
                            purple: '#7f00ff'
                        }
                    }
                }
            }
        }
    </script>

    <!-- GSAP & ScrollTrigger CDN -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/gsap.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/ScrollTrigger.min.js"></script>
    
    <!-- Three.js CDN -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/three.js/r128/three.min.js"></script>

    <style>
        body {
            background-color: #030712;
            color: #f9fafb;
            overflow-x: hidden;
            font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;
        }

        /* Custom Cursor Follower */
        .cursor-glow {
            pointer-events: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 400px;
            height: 400px;
            border-radius: 50%;
            background: radial-gradient(circle, rgba(0, 242, 254, 0.15) 0%, rgba(127, 0, 255, 0.05) 50%, transparent 70%);
            transform: translate(-50%, -50%);
            z-index: 9999;
            transition: transform 0.1s ease-out;
            will-change: transform;
        }

        /* Glassmorphism Card Style */
        .glass-card {
            background: rgba(17, 24, 39, 0.6);
            backdrop-filter: blur(16px);
            -webkit-backdrop-filter: blur(16px);
            border: 1px solid rgba(255, 255, 255, 0.08);
            transform-style: preserve-3d;
            perspective: 1000px;
        }

        /* Neon Text Glow */
        .neon-text-glow {
            text-shadow: 0 0 20px rgba(0, 242, 254, 0.6), 0 0 40px rgba(127, 0, 255, 0.4);
        }

        /* Hide Scrollbar */
        ::-webkit-scrollbar {
            width: 8px;
        }
        ::-webkit-scrollbar-track {
            background: #030712;
        }
        ::-webkit-scrollbar-thumb {
            background: #1f2937;
            border-radius: 4px;
        }
    </style>
</head>
<body class="antialiased selection:bg-brand-cyan selection:text-black">

    <!-- Cursor Light Glow -->
    <div id="cursor-glow" class="cursor-glow"></div>

    <!-- Intro Overlay (Cinematic Intro Sequence) -->
    <div id="intro-overlay" class="fixed inset-0 z-50 bg-black flex flex-col items-center justify-center overflow-hidden">
        <canvas id="intro-canvas" class="absolute inset-0 w-full h-full"></canvas>
        <div class="relative z-10 text-center px-4">
            <h1 id="intro-title" class="text-5xl md:text-8xl font-extrabold tracking-widest text-transparent bg-clip-text bg-gradient-to-r from-brand-cyan via-brand-violet to-brand-purple neon-text-glow opacity-0 scale-90">
                منصة الذكاء الاصطناعي
            </h1>
            <p id="intro-subtitle" class="text-gray-400 mt-4 text-lg md:text-xl font-mono tracking-wider opacity-0 transform translate-y-4">
                جاري تحميل المحرك الرسومي عالي الأداء...
            </p>
        </div>
        <div id="intro-progress-bar" class="absolute bottom-10 left-1/2 -translate-x-1/2 w-48 h-1 bg-gray-800 rounded-full overflow-hidden">
            <div id="intro-progress" class="w-0 h-full bg-gradient-to-r from-brand-cyan to-brand-purple"></div>
        </div>
    </div>

    <!-- Main Container -->
    <div id="main-content" class="relative opacity-0">

        <!-- Background 3D Canvas -->
        <div id="canvas-container" class="fixed inset-0 pointer-events-none z-0 opacity-40"></div>

        <!-- Navigation Bar -->
        <nav class="fixed top-0 left-0 right-0 z-40 px-6 py-4 flex justify-between items-center glass-card border-b border-gray-800/50">
            <div class="text-xl font-bold tracking-wider bg-clip-text text-transparent bg-gradient-to-r from-brand-cyan to-brand-purple">
                نيكسوس للذكاء الاصطناعي
            </div>
            <button class="relative group px-6 py-2 rounded-full overflow-hidden border border-brand-cyan/30 bg-transparent transition-all duration-300 hover:border-brand-cyan">
                <span class="absolute inset-0 w-full h-full bg-gradient-to-r from-brand-cyan/20 to-brand-purple/20 opacity-0 group-hover:opacity-100 transition-opacity duration-300"></span>
                <span class="relative text-sm font-semibold text-gray-200 group-hover:text-white">بدء الاختبار</span>
            </button>
        </nav>

        <!-- Hero Section -->
        <section class="min-h-screen flex flex-col justify-center items-center relative z-10 px-4 pt-20">
            <div class="text-center max-w-4xl mx-auto space-y-6">
                <div class="inline-block px-4 py-1.5 rounded-full border border-brand-cyan/30 bg-brand-cyan/5 text-brand-cyan text-xs font-mono uppercase tracking-widest hero-reveal">
                    اختبار أداء الرسومات والتفاعلات v2.0
                </div>
                <h1 class="hero-reveal text-5xl md:text-8xl font-black tracking-tight leading-tight text-white">
                    حركات سينمائية <br>
                    <span class="text-transparent bg-clip-text bg-gradient-to-r from-brand-cyan via-brand-violet to-brand-purple">
                        عالية التعقيد
                    </span>
                </h1>
                <p class="hero-reveal text-lg md:text-xl text-gray-400 max-w-2xl mx-auto font-light leading-relaxed">
                    اختبار قوة الرندر، استقرار الإطارات، وفيزياء التفاعل الثقيلة المصممة بالكامل عبر نماذج الذكاء الاصطناعي.
                </p>
                <div class="hero-reveal flex justify-center gap-4 pt-4">
                    <button class="px-8 py-4 rounded-xl bg-gradient-to-r from-brand-cyan to-brand-purple text-black font-bold shadow-lg shadow-brand-cyan/20 transform hover:-translate-y-1 transition-all duration-200">
                        استكشف المؤثرات
                    </button>
                    <button class="px-8 py-4 rounded-xl glass-card text-white font-semibold hover:bg-white/10 transition-all duration-200 border border-gray-700">
                        عرض البرمجيات
                    </button>
                </div>
            </div>
        </section>

        <!-- Interactive 3D Physics Cards Section -->
        <section class="py-32 relative z-10 px-6 max-w-7xl mx-auto">
            <div class="text-center mb-16 space-y-2">
                <h2 class="text-3xl md:text-5xl font-bold text-white section-title">مصفوفة الفيزياء التفاعلية</h2>
                <p class="text-gray-400 font-mono text-sm section-title">حرك الماوس فوق البطاقات لاختبار الاستجابة ثلاثية الأبعاد</p>
            </div>

            <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
                <!-- 3D Card 1 -->
                <div class="tilt-card glass-card p-8 rounded-2xl cursor-pointer group relative overflow-hidden transition-all duration-300">
                    <div class="absolute -left-10 -bottom-10 w-40 h-40 bg-brand-cyan/10 rounded-full blur-3xl group-hover:bg-brand-cyan/30 transition-all duration-500"></div>
                    <div class="text-brand-cyan font-mono text-xs mb-4">01 // محرك الرندر</div>
                    <h3 class="text-2xl font-bold text-white mb-2">تسريع الكارت (GPU)</h3>
                    <p class="text-gray-400 text-sm leading-relaxed mb-6">معالجة الحركات بمعدل 60 إطاراً في الثانية مع تحويل أحمال الجسيمات مباشرة إلى معالج الرسومات.</p>
                    <div class="w-full h-1 bg-gray-800 rounded-full overflow-hidden">
                        <div class="w-3/4 h-full bg-brand-cyan"></div>
                    </div>
                </div>

                <!-- 3D Card 2 -->
                <div class="tilt-card glass-card p-8 rounded-2xl cursor-pointer group relative overflow-hidden transition-all duration-300">
                    <div class="absolute -left-10 -bottom-10 w-40 h-40 bg-brand-violet/10 rounded-full blur-3xl group-hover:bg-brand-violet/30 transition-all duration-500"></div>
                    <div class="text-brand-violet font-mono text-xs mb-4">02 // فيزياء الحركة</div>
                    <h3 class="text-2xl font-bold text-white mb-2">ديناميكية السوائل</h3>
                    <p class="text-gray-400 text-sm leading-relaxed mb-6">مجالات قوة متجهة تفاعلية تستجيب بدقة وحرية لموقع المؤشر في الوقت الفعلي.</p>
                    <div class="w-full h-1 bg-gray-800 rounded-full overflow-hidden">
                        <div class="w-5/6 h-full bg-brand-violet"></div>
                    </div>
                </div>

                <!-- 3D Card 3 -->
                <div class="tilt-card glass-card p-8 rounded-2xl cursor-pointer group relative overflow-hidden transition-all duration-300">
                    <div class="absolute -left-10 -bottom-10 w-40 h-40 bg-brand-purple/10 rounded-full blur-3xl group-hover:bg-brand-purple/30 transition-all duration-500"></div>
                    <div class="text-brand-purple font-mono text-xs mb-4">03 // تسلسل GSAP</div>
                    <h3 class="text-2xl font-bold text-white mb-2">التزامن الإخراجي</h3>
                    <p class="text-gray-400 text-sm leading-relaxed mb-6">مُحفزات التمرير المتزامنة التي تقدم انتقالات ويب بدرجة إخراجية فائقة الجودة.</p>
                    <div class="w-full h-1 bg-gray-800 rounded-full overflow-hidden">
                        <div class="w-full h-full bg-brand-purple"></div>
                    </div>
                </div>
            </div>
        </section>

        <!-- Footer -->
        <footer class="py-12 border-t border-gray-800/50 text-center text-gray-500 font-mono text-xs relative z-10">
            جميع الأنظمة تعمل بكفاءة عالية &bull; NEXUS AI 2026
        </footer>
    </div>

    <!-- Animation & Logic Scripts -->
    <script>
        gsap.registerPlugin(ScrollTrigger);

        // --- Custom Cursor Logic ---
        const cursorGlow = document.getElementById('cursor-glow');
        window.addEventListener('mousemove', (e) => {
            gsap.to(cursorGlow, {
                x: e.clientX,
                y: e.clientY,
                duration: 0.4,
                ease: "power2.out"
            });
        });

        // --- 1. Intro Canvas Animation ---
        const introCanvas = document.getElementById('intro-canvas');
        const ctx = introCanvas.getContext('2d');
        introCanvas.width = window.innerWidth;
        introCanvas.height = window.innerHeight;

        let particlesArray = [];
        const numberOfParticles = 120;

        class Particle {
            constructor() {
                this.x = Math.random() * introCanvas.width;
                this.y = Math.random() * introCanvas.height;
                this.size = Math.random() * 2 + 0.5;
                this.speedX = Math.random() * 2 - 1;
                this.speedY = Math.random() * 2 - 1;
            }
            update() {
                this.x += this.speedX;
                this.y += this.speedY;
                if (this.x > introCanvas.width) this.x = 0;
                if (this.x < 0) this.x = introCanvas.width;
                if (this.y > introCanvas.height) this.y = 0;
                if (this.y < 0) this.y = introCanvas.height;
            }
            draw() {
                ctx.fillStyle = '#00f2fe';
                ctx.beginPath();
                ctx.arc(this.x, this.y, this.size, 0, Math.PI * 2);
                ctx.fill();
            }
        }

        function initIntroCanvas() {
            particlesArray = [];
            for (let i = 0; i < numberOfParticles; i++) {
                particlesArray.push(new Particle());
            }
        }

        function animateIntroCanvas() {
            ctx.clearRect(0, 0, introCanvas.width, introCanvas.height);
            for (let i = 0; i < particlesArray.length; i++) {
                particlesArray[i].update();
                particlesArray[i].draw();
            }
            requestAnimationFrame(animateIntroCanvas);
        }

        initIntroCanvas();
        animateIntroCanvas();

        // --- 2. GSAP Intro Timeline ---
        const introTimeline = gsap.timeline();

        introTimeline
            .to("#intro-progress", { width: "100%", duration: 1.5, ease: "power2.inOut" })
            .to("#intro-title", { opacity: 1, scale: 1, duration: 1, ease: "back.out(1.7)" }, "-=0.8")
            .to("#intro-subtitle", { opacity: 1, y: 0, duration: 0.8, ease: "power3.out" }, "-=0.4")
            .to("#intro-overlay", { 
                yPercent: -100, 
                duration: 1.2, 
                ease: "power4.inOut", 
                delay: 0.5,
                onComplete: () => {
                    document.getElementById('intro-overlay').style.display = 'none';
                }
            })
            .to("#main-content", { opacity: 1, duration: 0.8 }, "-=0.6")
            .from(".hero-reveal", { 
                y: 50, 
                opacity: 0, 
                duration: 1, 
                stagger: 0.15, 
                ease: "power3.out" 
            }, "-=0.2");

        // --- 3. Three.js Background ---
        const container = document.getElementById('canvas-container');
        const scene = new THREE.Scene();
        const camera = new THREE.PerspectiveCamera(75, window.innerWidth / window.innerHeight, 0.1, 1000);
        camera.position.z = 5;

        const renderer = new THREE.WebGLRenderer({ alpha: true, antialias: true });
        renderer.setSize(window.innerWidth, window.innerHeight);
        container.appendChild(renderer.domElement);

        const particlesGeometry = new THREE.BufferGeometry();
        const count = 1500;
        const positions = new Float32Array(count * 3);

        for (let i = 0; i < count * 3; i++) {
            positions[i] = (Math.random() - 0.5) * 15;
        }

        particlesGeometry.setAttribute('position', new THREE.BufferAttribute(positions, 3));

        const particlesMaterial = new THREE.PointsMaterial({
            size: 0.02,
            color: 0x00f2fe,
            transparent: true,
            opacity: 0.8
        });

        const particleSystem = new THREE.Points(particlesGeometry, particlesMaterial);
        scene.add(particleSystem);

        let mouseX = 0;
        let mouseY = 0;

        window.addEventListener('mousemove', (e) => {
            mouseX = (e.clientX / window.innerWidth) - 0.5;
            mouseY = (e.clientY / window.innerHeight) - 0.5;
        });

        function animateThree() {
            requestAnimationFrame(animateThree);
            particleSystem.rotation.y += 0.001;
            particleSystem.rotation.x += 0.0005;

            camera.position.x += (mouseX * 2 - camera.position.x) * 0.05;
            camera.position.y += (-mouseY * 2 - camera.position.y) * 0.05;
            camera.lookAt(scene.position);

            renderer.render(scene, camera);
        }
        animateThree();

        // --- 4. 3D Tilt Cards Effect ---
        const cards = document.querySelectorAll('.tilt-card');
        cards.forEach(card => {
            card.addEventListener('mousemove', (e) => {
                const rect = card.getBoundingClientRect();
                const x = e.clientX - rect.left;
                const y = e.clientY - rect.top;
                
                const centerX = rect.width / 2;
                const centerY = rect.height / 2;
                
                const rotateX = ((y - centerY) / centerY) * -12;
                const rotateY = ((x - centerX) / centerX) * 12;

                gsap.to(card, {
                    transform: `perspective(1000px) rotateX(${rotateX}deg) rotateY(${rotateY}deg) scale3d(1.02, 1.02, 1.02)`,
                    duration: 0.3,
                    ease: "power2.out"
                });
            });

            card.addEventListener('mouseleave', () => {
                gsap.to(card, {
                    transform: `perspective(1000px) rotateX(0deg) rotateY(0deg) scale3d(1, 1, 1)`,
                    duration: 0.5,
                    ease: "power2.out"
                });
            });
        });

        // --- 5. ScrollTrigger Animations ---
        gsap.from(".section-title", {
            scrollTrigger: {
                trigger: ".section-title",
                start: "top 80%",
            },
            y: 40,
            opacity: 0,
            duration: 1,
            stagger: 0.2,
            ease: "power3.out"
        });

        gsap.from(".tilt-card", {
            scrollTrigger: {
                trigger: ".tilt-card",
                start: "top 85%",
            },
            y: 60,
            opacity: 0,
            duration: 1,
            stagger: 0.2,
            ease: "power3.out"
        });

        window.addEventListener('resize', () => {
            camera.aspect = window.innerWidth / window.innerHeight;
            camera.updateProjectionMatrix();
            renderer.setSize(window.innerWidth, window.innerHeight);
            
            introCanvas.width = window.innerWidth;
            introCanvas.height = window.innerHeight;
        });
    </script>
</body>
</html>
